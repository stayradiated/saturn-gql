const { type, typeMutation, typeQuery } = require('./type')
const { queries, mutations, resolvers } = require('./queries')

module.exports = {
  type,
  typeMutation,
  typeQuery,
  queries,
  mutations,
  resolvers,
}
